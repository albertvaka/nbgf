#pragma once

namespace Window
{
	constinit const char* WINDOW_TITLE = "Gaem";

	// Resolution in virtual pixels (gets scaled preserving aspect ratio)
	constinit const int GAME_HEIGHT = 50 * 16;
	constinit const int GAME_WIDTH = 80 * 16;
}
