#pragma once

#include <array>

#include "partsys.h"

namespace Particles
{
	inline PartSys polvito(nullptr);
	inline PartSys bullet(nullptr);
	inline PartSys health(nullptr);
	inline PartSys missile(nullptr);

	void Init();
	void ClearAll();

};

